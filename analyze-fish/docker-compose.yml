version: '3.3'
services:
    analyze-fish:
        build: .
        volumes:
            - ./:/usr/src/app
        ports:
            - "80:80"
        deploy:
          resources:
            reservations:
              devices:
                - driver: nvidia
                  count: 1
                  capabilities: [gpu]
